---
# tasks file for rabbitmq
- name: Ensure magnum user is present
  rabbitmq_user: 
    user: magnum
    password: "{{ magnum_rabbit_password }}" 
    vhost: /
    configure_priv: .*
    read_priv: .*
    write_priv: .*
    state: present
  tags:
    - magnum
