---
# tasks file for rabbitmq
- name: Ensure zun user is present
  rabbitmq_user: 
    user: zun
    password: "{{ zun_rabbit_password }}" 
    vhost: /
    configure_priv: .*
    read_priv: .*
    write_priv: .*
    state: present
  tags:
    - zun
