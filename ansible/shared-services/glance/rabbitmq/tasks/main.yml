---
# tasks file for rabbitmq
- name: Ensure glance user is present
  rabbitmq_user: 
    user: glance
    password: "{{ glance_rabbit_password }}" 
    vhost: /
    configure_priv: .*
    read_priv: .*
    write_priv: .*
    state: present
  tags:
    - glance
