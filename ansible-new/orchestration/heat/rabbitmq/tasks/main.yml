---
# tasks file for rabbitmq
- name: Ensure heat user is present
  rabbitmq_user: 
    user: heat
    password: "{{ heat_rabbit_password }}" 
    vhost: /
    configure_priv: .*
    read_priv: .*
    write_priv: .*
    state: present
  tags:
    - heat
