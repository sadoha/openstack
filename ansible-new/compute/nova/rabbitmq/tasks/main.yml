---
# tasks file for rabbitmq
- name: Ensure nova user is present
  rabbitmq_user: 
    user: nova
    password: "{{ nova_rabbit_password }}" 
    vhost: /
    configure_priv: .*
    read_priv: .*
    write_priv: .*
    state: present
  tags:
    - nova
