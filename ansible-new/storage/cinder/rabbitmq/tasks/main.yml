---
# tasks file for rabbitmq
- name: Ensure cinder user is present
  rabbitmq_user: 
    user: cinder
    password: "{{ cinder_rabbit_password }}" 
    vhost: /
    configure_priv: .*
    read_priv: .*
    write_priv: .*
    state: present
  tags:
    - cinder
